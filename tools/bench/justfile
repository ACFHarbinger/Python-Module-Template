# tools/bench — run benchmarks for C++.

set shell := ["bash", "-c"]

all: cpp

cpp:
    cmake -S . -B build -DCMAKE_BUILD_TYPE=Release -DBUILD_BENCHMARK=ON && cmake --build build --target single_module_template_benchmark -j && ./build/benchmark/single_module_template_benchmark

# Dependency Policy

## Guidelines

1. **Security**: All dependencies are continuously audited via Dependabot.
2. **Licensing**: Dependencies must use GPL-compatible open source licenses (MIT, Apache-2.0, BSD, AGPL-3.0).
3. **Pinning**: Version requirements must use flexible semantic version bounds in `pyproject.toml`.
